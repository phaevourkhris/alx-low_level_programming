#include <stdio.h>
/**
 * main - Entry
 * Return: Always 0
 */
int main(void)
{
	char y;

	int z;

	y = 'a';
	z = 0;
	while
		(z < 10) {
			putchar(z + '0');
			z++;
		}
	while
		(y <= 'f') {
			putchar(y);
			y++;
		}
	putchar('\n');
	return (0);
}
